---
const base = import.meta.env.BASE_URL;
---

<footer>
  <div class="container footer-content">
    <div class="footer-grid">
      <!-- About Column -->
      <div class="footer-column">
        <h3>About Dila</h3>
        <p>Dila is a 501(c)(3) nonprofit organization working to improve environmental health in East Africa through volunteer action, education, and community support.</p>
      </div>

      <!-- Quick Links Column -->
      <div class="footer-column">
        <h3>Quick Links</h3>
        <ul class="footer-links">
          <li><a href={`${base}about`}>About</a></li>
          <li><a href={`${base}programs`}>Programs</a></li>
          <li><a href={`${base}impact`}>Impact</a></li>
          <li><a href={`${base}get-involved`}>Get Involved</a></li>
        </ul>
      </div>

      <!-- Resources Column -->
      <div class="footer-column">
        <h3>Resources</h3>
        <ul class="footer-links">
          <li><a href={`${base}privacy`}>Privacy Policy</a></li>
          <li><a href={`${base}contact`}>Contact</a></li>
          <li><a href={`${base}impact`}>Annual Reports</a></li>
        </ul>
      </div>

      <!-- Contact Column -->
      <div class="footer-column">
        <h3>Connect</h3>
        <p>Email: info@dilacares.org</p>
        <div class="social-links">
          <a href="#" aria-label="Facebook">Facebook</a>
          <a href="#" aria-label="Twitter">Twitter</a>
          <a href="#" aria-label="Instagram">Instagram</a>
          <a href="#" aria-label="LinkedIn">LinkedIn</a>
        </div>
      </div>
    </div>

    <!-- Copyright -->
    <div class="footer-bottom">
      <p>&copy; {new Date().getFullYear()} Dila. All rights reserved.</p>
    </div>
  </div>
</footer>

<style>
  footer {
    background: var(--dark-navy);
    color: white;
    padding: 4rem 0 2rem;
    border-top: 2px solid var(--cyan);
    margin-top: 4rem;
  }

  .footer-content {
    color: white;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .footer-column h3 {
    color: var(--cyan);
    margin-bottom: 1rem;
    font-size: 1.2rem;
  }

  .footer-column p {
    line-height: 1.6;
    opacity: 0.9;
  }

  .footer-links {
    list-style: none;
    padding: 0;
  }

  .footer-links li {
    margin-bottom: 0.5rem;
  }

  .footer-links a {
    color: white;
    opacity: 0.9;
    transition: all 0.3s ease;
  }

  .footer-links a:hover {
    color: var(--cyan);
    opacity: 1;
    padding-left: 5px;
  }

  .social-links {
    display: flex;
    gap: 1rem;
    margin-top: 1rem;
  }

  .social-links a {
    color: white;
    opacity: 0.9;
    transition: all 0.3s ease;
  }

  .social-links a:hover {
    color: var(--cyan);
    opacity: 1;
  }

  .footer-bottom {
    text-align: center;
    padding-top: 2rem;
    border-top: 1px solid rgba(0, 243, 255, 0.3);
    opacity: 0.8;
  }

  @media (max-width: 768px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }
  }
</style>